# Asset Weaver

A frontend application for managing and tokenizing assets on a Substrate blockchain.

## Getting started

- Install dependencies with `bun install`
- Run the development server with `bun dev`
- Build the project with `bun build`
